var inquirer = require("inquirer")

function Word() {
  this.rightGuesses = rGuesses;
  this.wronguesses = wGuesses;
  this.words = word;
  this.letters = letters
}

inquirer.prompt([
  {
    message: "would you like to play a game ?"
  }

]).
